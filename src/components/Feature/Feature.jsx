import PropTypes from 'prop-types';


const Feature = ({feature}) => {
    return (
        <div>
            
        </div>
    );
};

Feature.PropTypes = {
    feature: PropTypes.string
}

export default Feature;