import PropTypes from 'prop-types';


const Feature = () => {
    return (
        <div>
            
        </div>
    );
};

Feature.PropTypes = {
    feature: PropTypes.object
}

export default Feature;